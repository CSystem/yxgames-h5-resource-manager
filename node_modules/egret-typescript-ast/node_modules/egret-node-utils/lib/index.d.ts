export declare function walk(path: string, filter: (path) => boolean): Promise<Array<string>>;
import * as cp from 'child_process';
export declare var shell: (path: string, args: string[], opt: cp.SpawnOptions) => Promise<number>;
